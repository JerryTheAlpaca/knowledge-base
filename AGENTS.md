# AGENTS.md

本文件约定本仓库中 AI 编程助手的工作方式。以用户当前明确要求为准；开发文档用于理解设计，不要把其中某次对话的交付范围当成永久限制。

## 项目目标与范围

这是一个把公众号、小红书、B 站、网页、AI 对话和个人灵感，自动整理到用户自己 Obsidian 中的知识摄入系统。

- 用户电脑关机时，服务器也能完成内容提取和 AI 加工。
- 每个人使用自己的模型 API Key；服务器按用户加密托管并调用，不能回退到其他用户的 Key。
- 每个人的 Vault 在自己的电脑；插件上线后下载原始材料和加工结果，不把整个 Vault 上传到服务器。
- 当前服务器为 2 核 2GB，可升级到 2 核 4GB；个人每天约 5–10 条内容，视频通常约 20 分钟。
- 部署保持云厂商无关，支持以后从腾讯云迁移到阿里云。

实现顺序以用户当前任务为先，参考“可靠接收 → 云端加工 → 本地入库 → 来源适配”。不要把一项小需求扩成整套系统建设。

## 阅读文档

首次进入项目先读 `README.md`，再按当前任务读取相关部分，避免每轮重复通读全部文档。

- `docs/01-方案评审与架构决策.md`：方案取舍与约束。
- `docs/02-开发设计文档.md`：架构、接口、数据、加工、投递和部署。
- `docs/03-开发任务与验收清单.md`：实施阶段和验收场景。
- `docs/04-B站字幕获取专项设计.md`：字幕获取、分 P、登录限制和降级路径。

代码、文档与新需求不一致时，结合当前任务作出合理判断；影响接口或行为的决定要同步到相关文档。不要为普通文案或实现细节改动重写整套文档。

## 工作方式

- 需求明确就直接推进。正常代码修改、相关文件调整、小范围重构和问题修复，无需反复询问是否继续。
- 对可逆、局部的实现选择自行判断，简短说明重要假设即可。
- 只有缺少会改变方案的关键信息，或涉及尚未授权的生产操作、不可恢复的数据删除等，才提出必要问题；已经得到的授权不要重复索取。
- 先完成用户需要的最小完整功能，再处理与它直接相关的问题。无关改进可以记录，不顺手扩大范围。
- 大项目组织多 Agent 并行开发。
- 使用中文沟通。较长工作给出简短进展，重点说清发现、决定和剩余问题。

## 实现原则

- 优先简单、直接、可维护的代码，复用现有结构和依赖。边界清楚即可，不为可能出现的未来需求提前搭框架。
- 首版服务端采用 FastAPI、独立 Python Worker 和 SQLite；客户端采用 TypeScript Obsidian 插件。按实际代码与任务需要选取相关部分，不要求每次改动覆盖全栈。
- 持久化任务与耗时请求分开；网页、字幕和模型调用不要占用长数据库事务。
- 不默认引入 Redis、Celery、微服务、向量数据库、复杂 Agent 编排或常驻浏览器。新增依赖应解决当前具体问题。
- 来源适配和模型调用保留必要接口，不给每个小函数套抽象层，不维护没有实际使用方的兼容逻辑。
- 保留原始材料，明确区分已接收、已整理和已入库；AI 失败不应使已保存材料消失。
- B 站按当前视频及分 P/cid 取字幕。弹幕不是字幕，画面上的文字也不等于可下载字幕轨；需要登录或获取失败时如实标记。
- 不用标题、简介或模型猜测冒充原文。来源观点、用户备注和 AI 候选启发分开。
- 本地导入可重试且不重复建笔记；更新生成内容时保护用户自己的修改。

## 不要过度防御

- 校验主要放在真实边界：外部请求、来源文件、模型结果、凭据访问和写盘路径。内部已由类型与调用约定保证的值，不层层重复检查。
- 针对实际可能发生的失败处理，不为假想场景堆积分支、配置、备用链路和通用错误框架。
- 不用到处 `try/except` 或 `try/catch` 吞掉问题；在能恢复或能给出有用错误的边界处理异常。
- 不添加多余的确认弹窗、警告页、审批步骤或强制填写字段。普通采集保持低摩擦。
- 用户隔离、凭据不泄露、预算不串用、原始材料不被误删、人工笔记不被覆盖，是正确性要求；按既定边界实现即可，不把每次改动扩成全面安全审计。

## 验证与测试

**不要求每次修改代码都跑测试，也不要求每轮工作都跑全量测试。** 根据改动影响选择验证方式，在一个完整改动完成后集中验证即可。

- 文档、注释、文案、样式以及其他低风险局部调整，通常检查相关内容或 diff 就够了，不必运行测试。
- 普通业务改动优先复用已有检查，选择能覆盖受影响行为的小范围测试、类型检查、构建或一次手动验证，不机械地全部执行。
- 修改用户隔离、凭据处理、预算扣减、任务恢复、投递回执、文件删除或笔记覆盖逻辑时，做针对相关故障场景的验证。不要仅因这些模块存在，就把它们全测一遍。
- 修复明确缺陷时，若已有测试不能防止复发且回归风险值得覆盖，再补有意义的测试。
- 不为简单可逆改动、私有函数的实现细节或提高覆盖率数字而写测试；不要让测试只重复实现本身。
- 测试通过后，除非有新改动、失败或未解决的具体问题，不重复运行同一组检查。
- `开发任务与验收清单` 用于相应里程碑和发布验收，不是每次改动的必跑清单。已有 CI 或项目明确要求的发布检查仍应执行。
- 只报告实际做过的验证。没有必要跑测试时可以直接说明“本次为文档/低风险调整，未运行测试”，不需要因此暂停任务或反复请求确认。

## 文件与交付

- 修改前查看相关文件，保留用户已有改动，不覆盖与任务无关的内容。
- 搜索优先用 `rg`；批量读取独立文件，避免无目的地反复扫描仓库。
- Windows 下使用原生 PowerShell 文件操作与明确路径，不跨 shell 拼接删除命令。
- 不把真实 API Key、Cookie、私人对话或生产 Vault 写进代码、示例、日志或测试仓库。
- 接口、数据字段或重要行为发生变化时，更新直接相关的文档和示例。
- 完成后简短说明改了什么、做了哪些必要验证、还有什么实际限制；不要把未执行的测试、未上线的功能或未经实测的平台成功率说成已完成。
- 完成后清除临时文件，保持工作区干净。
