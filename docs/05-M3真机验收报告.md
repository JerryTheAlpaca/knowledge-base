# M3 真机验收报告（2026-09-08）

## 范围与方法

M3 本地入库（Obsidian 插件）真机验收，对应验收清单场景 **A09–A12** 及 M3 附加行为项。

- 环境：Windows 桌面 Obsidian（kb-test-vault），生产服务器 https://kb.jerrythealpaca.cn（commit 06afaaa 部署）
- 方法：每场景在服务器用 API 造验收专用条目（不动真实数据），插件在真机 Vault 上走完整同步链路，本地落盘 + 服务器 SQLite 双向核对断言
- A09 中断制造：监视 `.staging-*` 目录出现即强杀 Obsidian 进程（80MB 大附件拉长下载窗口至分钟级，5MB 窗口 <1s 抓不住）

## 场景结果

| 场景 | 内容 | 结果 |
|---|---|---|
| 基础链路 | 配对 → 领取成品 → 笔记结构/回执 | ✅ 通过（修复 bug #1 后） |
| A09 | 下载一半退出 Obsidian，恢复后完整提交 | ✅ 通过（80MB 附件条目：强杀后无笔记/无 commit/无 receipt/staging 半成品；重启后重新下载，附件 83,886,080 字节完整落盘、SHA 校验、单条笔记、服务器恰 1 条 receipt、staging 清理） |
| A10 | ACK 丢失后补发回执，服务器幂等 | ✅ 通过（修复 bug #5 后；ack_sent false→true，服务器 (item,revision) 幂等，插件设备 receipt 不重复） |
| A11 | 用户改备注后新版本到达，备注保留 | ✅ 通过（r4→r5→r6 多轮 reprocess，备注完整保留、生成区正常合并、单条笔记） |
| A12 | 改过生成区再生成，新结果进待合并区 | ✅ 通过（生成区原样保留、kb_status=merge_needed、conflicts/ 文件生成且证据链接指向新 bundle） |
| 附加：删除停止复建 | 删除 Source 笔记 → suppression | ✅ 通过（真机插件自动判定"用户删除了 Source 笔记"，suppression.json 记录，不复建） |
| 附加：恢复重建 | 恢复命令 → 重建 | ⚠️ 半通过，发现设计缺口（见遗留问题 #7），未闭环 |

## 发现并修复的 bug（真机验收价值所在）

插件侧五项 + 服务端一项，均为真机跑通才发现的问题：

1. **user_note 读取层级错误**（engine.ts）：capture.json 结构为 `{capture: {..., user_note}}`，插件读顶层字段永远拿到 null →「我的备注」显示（无）。修复为读 `capture.user_note`。
2. **processNote 传 string 路径崩溃**（vaultfs.ts）：Obsidian `vault.process` 内部把 string 当 TFile 用（`file.saving = true`）抛 `Cannot create property 'saving' on string`，**所有"更新已有笔记"路径必然失败**（新建不受影响）。修复为先解析 TFile 再调用，拿不到走 read+write 兜底。
3. **冲突误判**（engine.ts/template.ts）：`generated_digest` 是 SHA-256，比对时却拿原文比哈希——恒不相等，**每次版本更新都被误判为冲突**进 conflicts/。修复为 pre-read 算哈希 + 回调内同步比对。
4. **双 frontmatter**（template.ts）：`mergeKbFrontmatter` 拼接时丢失结尾 `---`，下次更新识别不到完整 frontmatter 又包一层，双 frontmatter 层层叠加。修复 + smoke 补断言（原断言只查 endsWith 未查结尾 `---`，拦不住）。
5. **启动补发顺序**（main.ts）：`recoverReceipts()` 在 `refreshToken()` 之前执行，tokenCache 为 null 时补发静默 return 0——**启动回执补发是死代码**。修复为先刷新 Token 再补发。
6. **服务端 enrich 丢附件**（enrich.py `_base_bundle_files`）：按 relative_path 白名单只挑 3 个基础文件，`original_submission` role 的上传附件被过滤——**用户上传的原始材料永远不进 bundle**。修复为按 role（original_submission + source_material）筛选。服务端测试 70/70 通过后部署。

提交：06afaaa（六项修复 + smoke 断言），已部署生产（health/ready 全绿）。

## 遗留问题

7. **恢复命令语义缺口（未修复）**：「恢复被删除条目的自动重建」只清 suppression.json；但只要本地 commit 存在且笔记仍处于删除状态，下一个新版本事件会**再次触发 suppression**——恢复命令实际无法让笔记重建。修复方向：unsuppressAll 时同步删除对应条目的本地 commit 记录（或标记重置），让下一次事件走全新建路径。实测记录：15:51 suppress → 15:53 清空（疑似执行过恢复命令）→ 16:03 r5 事件重新 suppress。
8. **Obsidian GPU 进程间歇崩溃**（环境问题，非插件）：命令行启动反复失败（`GPU process isn't usable`），手动打开偶发闪退，但存活的实例完整跑通了同步。未影响验收结论；若持续可考虑更新显卡驱动或 Obsidian 安装器（当前 1.13.7）。
9. **staging 空目录残留**（低优先级）：Obsidian `adapter.list` 只返回文件不返回空目录，staging 清理后遗留 `.staging-*` 空壳（下一次下载同条目时会复用/清理，无正确性影响）。headless 复刻时同样复现。

## 验收资产与清理说明

服务器验收条目（可删）：7c1d425a（M3 基础/A11/A12）、5785cbc3（A09 5MB）、445748ad（A09 80MB + suppression）。kb-test-vault 内 `99 System/KnowledgeInbox/suppression.json` 当前含 445748ad 抑制记录，属正常状态。

无头验证工具沉淀：`apps/obsidian-plugin/scripts/recovery-headless.ts` + `obsidian-stub.ts`（Node 直接驱动 SyncEngine 处理 pending，可复用于插件故障排查，需解决 token 取回）。
